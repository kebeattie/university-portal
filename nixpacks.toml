# Nixpacks configuration for Railway deployment
[phases.setup]
nixPkgs = ['...', 'sqlite']

[phases.build]
cmds = ['dotnet publish -c Release -o out']

[start]
cmd = 'cd out && dotnet UniversityPortal.dll --urls=http://0.0.0.0:$PORT'
