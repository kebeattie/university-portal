# Nixpacks configuration for Railway deployment
[phases.setup]
nixPkgs = ['dotnet-sdk_8', 'sqlite']

[phases.install]
cmds = ['dotnet restore UniversityPortal.csproj']

[phases.build]
cmds = ['dotnet publish UniversityPortal.csproj -c Release -o /app/out']

[start]
cmd = 'dotnet /app/out/UniversityPortal.dll --urls=http://0.0.0.0:$PORT'
